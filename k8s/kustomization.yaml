apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: lovetest-admin

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app: lovetest-admin
  version: v1.0.0

images:
  - name: omaticaya/lovetest-admin
    newTag: latest

namespace: default